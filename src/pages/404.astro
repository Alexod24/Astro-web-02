---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Error 404`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">Lo sentimos, no pudimos encontrar esta página.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Pero no te preocupes, puedes encontrar muchos otros recursos en nuestra página de inicio.
        </p>
        <!-- Barra de búsqueda -->
        <div class="mb-8">
          <input type="text" placeholder="Buscar..." class="border rounded px-4 py-2"/>
        </div>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Volver a la página de inicio</a>
        <p class="mt-4 text-sm text-gray-500">O echa un vistazo a nuestras <a href="/blog" class="text-blue-600 underline">últimas publicaciones del blog</a>.</p>
      </div>
      <!-- Ilustración opcional -->
      <img src="/path/to/your/illustration.png" alt="Ilustración 404" class="mt-8"/>
    </div>
  </section>
</Layout>
